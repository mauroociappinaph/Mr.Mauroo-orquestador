// V0 stub — cleaning cue types only
export type OfficeCleaningCue = {
  id: string;
  agentId: string;
  agentName: string;
  ts: number;
};

export type SessionEpochSnapshot = Record<string, number>;
